import { createSlice } from "@reduxjs/toolkit";

export const anzeigeSlice = createSlice({
    name: 'anzeige',
    initialState: {
        text: ''
    },
    reducers: {
        
    }
})

export default anzeigeSlice.reducer